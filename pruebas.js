// validad (recibe los links)
// for normal que recorre el arreglo 
// if(verificar si el arreglo tiene .https con .includes)
// const https= require('https');
/*const http = require('http');
 http.get()


 set time out sirve para hacer  quelas funciones se ejecuten 
 en el orden que yo le estoy dando 
 simula  sincronia 
 por buenas parcaticas  se llama una funcion de ntro de una funcion



 promesas///
 async funciones que quieres que se pongan en pausa
 await es lo que quieres que se ejecute cuando termine la funcion
new promise es una funcion 
tiene como argumentos (resolve y reject)
 const printObjects = ()=>{
     console.log(''entra a función de print )
     set time out puede recibir tres argumentros(resolve, 2000, 'bye')
     lo que me regresara cuanto  se va a esperar y  lo que va a hacer

     promise.all te trae todo lo que le pides al mismo tiempo en un arreglo
     promise all se espera  a que se cumplan todas las promesas

     pagina json place holder
     apis de prueba

     cuando quieres que algo se ejecute cuando todas las promesas se hayan ejecutado

     //// Async y await

     hacer ejercicios con async y await
     ver como  se van ejecutando las promesas para checar en que orden necesitoq ue se muestre la data 
     const url = 'https://...'

     const fetchData =()=>{
         const resp = await fetch(url);

     }
 }
 
 */